# Client API Base URL (e.g. '/api' for local proxy, or 'https://api.yourdomain.com/api' for production)
VITE_API_BASE_URL=/api

# Optional: Backend Proxy Target for Vite Dev Server
VITE_API_TARGET=http://localhost:5000
